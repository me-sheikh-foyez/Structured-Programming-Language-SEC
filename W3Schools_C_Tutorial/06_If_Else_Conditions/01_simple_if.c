#include <stdio.h>

/**
 * W3Schools C Tutorial: If Statement
 * Concept: Executing a block of code only when a condition is true.
 */
int main(void) {
    int x = 20;
    int y = 18;

    if (x > y) {
        printf("%d is greater than %d\n", x, y);
    }

    return 0;
}
