#include <stdio.h>

/**
 * W3Schools C Tutorial: While Loop
 * Concept: Executing code repeatedly as long as condition remains true.
 */
int main(void) {
    int i = 0;

    while (i < 5) {
        printf("Iteration: %d\n", i);
        i++;
    }

    return 0;
}
